{
  "name": "dewnote",
  "private": true,
  "version": "0.0.0",
  "description": "A minimal editor for notebooks written as markdown: prose, LaTeX maths, and code cells that run in the page.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/js-yaml": "^4.0.9",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
